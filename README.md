# 이미지 마커 오버레이 PoC

React 18 + Vite + Zustand + React Query + deck.gl 스터디용 PoC. 이미지 위에 mock API로 받은 마커(점/사각형)를 deck.gl로 오버레이합니다.

**실행**

```bash
npm install
npm run dev
```

브라우저에서 "데이터 불러오기"를 누르면 마커가 로드되고, 오버레이 on/off·마커 클릭 선택을 확인할 수 있습니다.
